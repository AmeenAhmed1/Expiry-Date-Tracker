<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.ameen.expirydatetracker.ui.fragment.HomeFragment"
        android:label="@string/home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_expireFragment"
            app:destination="@id/expireFragment" />
    </fragment>
    <fragment
        android:id="@+id/expireFragment"
        android:name="com.ameen.expirydatetracker.ui.fragment.ExpireFragment"
        android:label="@string/expire"
        tools:layout="@layout/fragment_expire" />
</navigation>